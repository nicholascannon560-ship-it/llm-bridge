{"action": "patch_file", "repo": "KalshiML", "path": "hourly_engine.py", "branch": "main", "ops": [{"replace": {"old": "    x, y = x[m], y[m]\n    k_ls = float((x * y).sum() / max((x * x).sum(), 1e-9))\n    k = (k_ls * len(x) + K_DIR_PRIOR * K_DIR_N0) / (len(x) + K_DIR_N0)\n    return min(1.0, max(0.0, k)), int(m.sum())", "new": "    x, y = x[m], y[m]\n    # robust slope: median per-hour ratio y/x. Least squares squares the big\n    # misses, so one extreme afternoon whipped k 0.43 -> 0.0 live on\n    # 2026-08-06 (temp crashed ~4F under the blend and the models' warm call\n    # was wrong); the median ratio shrugs off those hours instead.\n    ratios = y / x\n    k_ls = float(np.median(ratios[np.isfinite(ratios)]))\n    k = (k_ls * len(x) + K_DIR_PRIOR * K_DIR_N0) / (len(x) + K_DIR_N0)\n    return min(1.0, max(0.0, k)), int(m.sum())"}}, {"replace": {"old": "        triples = [(float(b), float(m), float(t))\n                   for b, m, t in zip(g[\"blend\"], g[\"model_mean\"], g[\"mid\"])\n                   if pd.notna(m)]", "new": "        g1 = g.groupby(\"hour_key\", as_index=False).first()   # ONE row per hour (file order is chronological); multi-scan duplicates otherwise count 4-5x in the k fit and residual bank\n        triples = [(float(b), float(m), float(t))\n                   for b, m, t in zip(g1[\"blend\"], g1[\"model_mean\"], g1[\"mid\"])\n                   if pd.notna(m)]"}}, {"replace": {"old": "            hrs = [str(h) for h, m in zip(g[\"hour_key\"], g[\"model_mean\"]) if pd.notna(m)]", "new": "            hrs = [str(h) for h, m in zip(g1[\"hour_key\"], g1[\"model_mean\"]) if pd.notna(m)]"}}, {"replace": {"old": "            for _, r in g.iterrows():\n                key = str(r[\"hour_key\"])\n                if key in done:\n                    continue", "new": "            for _, r in g1.iterrows():\n                key = str(r[\"hour_key\"])\n                if key in done:\n                    continue\n                done.add(key)"}}, {"replace": {"old": "                def _replay(scan_rows):\n                    cands = []\n                    for r in scan_rows:\n                        if r.get(\"yes_bid_c\") is None or not r.get(\"dist\") or r[\"dist\"] <= 0:\n                            continue\n                        yb = r[\"yes_bid_c\"] / 100.0\n                        no_cost = 1.0 - yb\n                        no_edge = yb - float(r[\"p_yes\"])\n                        if no_edge >= 0.10 and 0.10 <= no_cost <= 0.90:\n                            cands.append((no_edge, r, no_cost))\n                    if not cands:\n                        return None, None\n                    _, r, no_cost = max(cands, key=lambda c: c[0])\n                    no_won = mid < float(r[\"thr\"])\n                    return r[\"ticker\"], round(((1.0 if no_won else 0.0) - no_cost - 0.01) * 100.0, 2)", "new": "                def _replay(scan_rows):\n                    cands = []\n                    best = None\n                    for r in scan_rows:\n                        if r.get(\"yes_bid_c\") is None or not r.get(\"dist\") or r[\"dist\"] <= 0:\n                            continue\n                        yb = r[\"yes_bid_c\"] / 100.0\n                        no_cost = 1.0 - yb\n                        no_edge = yb - float(r[\"p_yes\"])\n                        if best is None or no_edge > best:\n                            best = no_edge\n                        if no_edge >= 0.10 and 0.10 <= no_cost <= 0.90:\n                            cands.append((no_edge, r, no_cost))\n                    if not cands:\n                        return None, None, best\n                    _, r, no_cost = max(cands, key=lambda c: c[0])\n                    no_won = mid < float(r[\"thr\"])\n                    return r[\"ticker\"], round(((1.0 if no_won else 0.0) - no_cost - 0.01) * 100.0, 2), best"}}, {"replace": {"old": "                for bk, grp in sorted(lead_groups.items()):\n                    t_tk, t_pnl = _replay(grp[0])        # earliest scan in the window\n                    by_lead[bk] = {\"trade\": t_tk, \"pnl_c\": t_pnl,\n                                   \"book\": any(r.get(\"yes_bid_c\") is not None for r in grp[0])}", "new": "                for bk, grp in sorted(lead_groups.items()):\n                    t_tk, t_pnl, t_best = _replay(grp[0])   # earliest scan in the window\n                    by_lead[bk] = {\"trade\": t_tk, \"pnl_c\": t_pnl,\n                                   \"best_edge_c\": (round(t_best * 100.0, 1) if t_best is not None else None),\n                                   \"book\": any(r.get(\"yes_bid_c\") is not None for r in grp[0])}"}}], "message": "v3.3: robust k (median ratio, one triple per hour) + dedupe grades/resid bank + best_edge_c per lead window"}