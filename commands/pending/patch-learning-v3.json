{"action": "patch_file", "repo": "KalshiML", "path": "learning.py", "branch": "main", "ops": [{"insert_after": {"anchor": "P_EPS              = 1e-4   # never let a model prob sit exactly at 0/1 (see clamp_p)", "text": "\nHOURLY_SIG_V_MIN     = 3      # v3.0 hourly reset: pre-v3 hourly evidence (sig_v<3) is quarantined from recompute"}}, {"replace": {"old": "            m = r.get(\"mins_to_formation\")\n            if m is not None and m < 0:\n                continue\n            n_kept += 1", "new": "            m = r.get(\"mins_to_formation\")\n            if m is not None and m < 0:\n                continue\n            # v3.0 HOURLY RESET: hourly evidence logged before the direction-aware\n            # pricing fix carried a systematically mispriced p_yes (obs-anchored\n            # blend ignored the models' directional call; -2.588c/ctr at ask,\n            # n=923, measured 2026-08-06). Only hourly rows stamped sig_v>=3 may\n            # feed cell states; old hourly cells vanish on this recompute. The\n            # raw log is untouched -- audit trail preserved, nothing learns from it.\n            if str(r.get(\"base_cell\", \"\")).startswith(\"hourly|\"):\n                try:\n                    if int(r.get(\"sig_v\") or 1) < HOURLY_SIG_V_MIN:\n                        continue\n                except (TypeError, ValueError):\n                    continue\n            n_kept += 1"}}], "message": "learning: quarantine pre-v3 hourly evidence (sig_v<3) from recompute_cell_states"}