# Copy to .env for local development. On Railway, set GITHUB_TOKEN as a
# service variable / secret instead of committing it.
GITHUB_TOKEN=ghp_your_personal_access_token_here

# Optional overrides
# PORT=8000
# GITHUB_TIMEOUT_SECONDS=30
